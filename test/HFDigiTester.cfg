process TEST = {

include "Validation/HcalDigis/test/HcalDigiTester.cfi"

  source = PoolSource {
     untracked vstring fileNames = {'file:data/simhits_pi+100_etaphi344.root'}
     untracked int32 maxEvents = -1
     untracked uint32 debugVebosity = 10
     untracked bool   debugFlag     = true
  }

  module hcalDigiAnalyzer = HcalDigiTester{
        untracked string outputFile = 'HcalDigisValidationHF.root'
        untracked string hcalselector	= 'HF'
	untracked bool subpedvalue	= false
  }

}